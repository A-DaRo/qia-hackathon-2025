# Network Topology Configuration (2-node setup)
# Reference: implementation_plan.md §3.2

nodes:
  - name: "Alice"
    qubits: 20              # Maximum qubits per node
    
  - name: "Bob"
    qubits: 20

links:
  - node_1: "Alice"
    node_2: "Bob"
    fidelity: 0.95          # EPR pair fidelity (controls QBER)
    length: 10              # Link length (km) - affects timing
